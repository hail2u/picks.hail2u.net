{
  "name": "picks.hail2u.net",
  "devDependencies": {
    "mustache": "^0.8.2",
    "pit-ro": "^0.0.1",
    "request": "^2.36.0"
  },
  "eslintConfig": {
    "env": {
      "node": true
    }
  },
  "private": true,
  "scripts": {
    "build": "node index.js",
    "deploy": "cd build && git push origin gh-pages",
    "postbuild": "git commit --all -m \"Build\" && git push origin master",
    "predeploy": "npm run build && cd build && git add --all && git commit -m \"Build\""
  }
}
