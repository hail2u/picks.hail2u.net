{
  "name": "picks.hail2u.net",
  "devDependencies": {
    "mustache": "^0.8.2",
    "request": "^2.36.0"
  },
  "eslintConfig": {
    "env": {
      "node": true
    }
  },
  "private": true,
  "scripts": {
    "build": "node index.js && git commit --all -m \"Build\" && git push origin master",
    "deploy": "npm run build && cd build && git add --all && git commit -m \"Build\" && git push origin gh-pages"
  }
}
